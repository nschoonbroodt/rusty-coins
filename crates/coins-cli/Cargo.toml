[package]
name = "coins-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
clap = { version = "4.5.39", features = ["derive", "env"] }
coins-core = { version = "0.1.0", path = "../coins-core" }
dotenvy = "0.15.7"
